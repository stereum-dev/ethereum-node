---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
  #  nimbus beacon logs
  - name: nimbus beacon
    command: "docker logs --tail=20 stereum-cd1efba0-1401-11ed-b7bc-3b8bd60e0aa1"
    register: nimbus_beacon
    until:
      - nimbus_beacon.stdout is search("Failed to send POST Request with JSON-RPC")
      - nimbus_beacon.stdout is search("Eth1 chain monitoring failure, restarting")
    retries: 60
    delay: 10

# EOF