---
- include_tasks: create-web-server-dir.yml

- name: Initialize a new npm project and install dependencies
  shell: |
    npm init -y
    npm install node-pty ws
  args:
    chdir: "{{ stereum.settings.controls_install_path | default(stereum.defaults.controls_install_path) }}/web-server/"

- include_tasks: create-serverjs-file.yml
#EOF
