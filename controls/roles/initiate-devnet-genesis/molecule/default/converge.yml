---
- name: Converge
  hosts: all
  vars_files:
    - ../../../../defaults/stereum_defaults.yaml

  tasks:
    - name: "Include copy-devnet-genesis"
      include_role:
        name: "copy-devnet-genesis"
      vars:
        working_dir: "{{ stereum_static.defaults.controls_install_path }}"
#EOF
