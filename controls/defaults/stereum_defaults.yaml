stereum_static:
  defaults:
    controls_install_path: /opt/stereum
    os_user: stereum
    updates:
      in_progress:
      lane: stable
      available:
      unattended:
        check: true
        install: false